@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/bitcoin-system-targets.cmake")

check_required_components(system)

# Propagate dependencies to consumers
include(CMakeFindDependencyMacro)

find_dependency(Boost 1.86 REQUIRED COMPONENTS
  iostreams
  locale
  program_options
  thread
  url
)

# If you support ICU conditionally
# find_dependency(PkgConfig)
# pkg_check_modules(ICU ucs4 QUIET)

# secp256k1 is frequently vendored or found differently – left optional here
